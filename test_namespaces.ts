import { Geometry } from "./namespaces"


const circleArea = Geometry.Circle.calculateArea(20)
const rectangleArea = Geometry.Rectangle.calculateArea(10, 4)
console.log(circleArea)
console.log(rectangleArea)
